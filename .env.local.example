NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
# Mobile (same Wi-Fi): npm run dev:mobile, then open http://<PC-IP>:3000
# API auto uses http://<PC-IP>:5000 when not on localhost
